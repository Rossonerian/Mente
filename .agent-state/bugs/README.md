# Bugs

Create bug task JSON in `.agent-state/tasks/` from the task template, set `type` to `BUG`, assign BUG-FIXER only after
a reproducible failure report exists, and store the report here as `<TASK-ID>.md`. Search existing tasks first.
